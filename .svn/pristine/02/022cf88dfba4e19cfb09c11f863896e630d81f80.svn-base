import { EgretLoadable } from "egret";
import ConstantList from "../../appConfig";
import { useTranslation, withTranslation, Trans } from 'react-i18next';
const EpidemiologicalFactors = EgretLoadable({
  loader: () => import("./EpidemiologicalFactors")
});
const ViewComponent = withTranslation()(EpidemiologicalFactors);

const EpidemiologicalFactorsRoutes = [
  {
    path:  ConstantList.ROOT_PATH+"dashboard/epidemiological-factors",
    exact: true,
    component: ViewComponent
  }
];

export default EpidemiologicalFactorsRoutes;