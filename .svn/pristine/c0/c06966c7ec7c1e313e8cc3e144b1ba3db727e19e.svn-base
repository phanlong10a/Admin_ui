import { EgretLoadable } from "egret";
import ConstantList from "../../appConfig";
import { useTranslation, withTranslation, Trans } from 'react-i18next';
const SuspectedLevelLevelTable = EgretLoadable({
  loader: () => import("./SuspectedLevelLevelTable")
});
const ViewComponent = withTranslation()(SuspectedLevelLevelTable);

const SuspectedLevelLevelRoutes = [
  {
    path:  ConstantList.ROOT_PATH+"dashboard/suspect_level",
    exact: true,
    component: ViewComponent
  }
];

export default SuspectedLevelLevelRoutes;
