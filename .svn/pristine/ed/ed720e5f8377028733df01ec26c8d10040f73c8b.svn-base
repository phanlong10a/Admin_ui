import { EgretLoadable } from "egret";
import ConstantList from "../../appConfig";
import { useTranslation, withTranslation, Trans } from 'react-i18next';
const SuspectedTypeTable = EgretLoadable({
  loader: () => import("./SuspectedTypeTable")
});
const ViewComponent = withTranslation()(SuspectedTypeTable);

const SuspectedTypeRoutes = [
  {
    path:  ConstantList.ROOT_PATH+"dashboard/suspect-type",
    exact: true,
    component: ViewComponent
  }
];

export default SuspectedTypeRoutes;
